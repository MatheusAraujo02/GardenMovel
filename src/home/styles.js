import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container:{
        width: '100%',
        height: '100%',
        padding: 13,
        backgroundColor: 'fff',
        border: '10px',
        color: 'blue',
    },
    texto:{
        color: 'fff',
    }

})

export default styles;